workflows:
  android-release:
    name: Android Build
    max_build_duration: 30
    environment:
      groups:
        - keystore
    scripts:
      - flutter pub get
      - flutter build apk --release
    artifacts:
      - build/app/outputs/flutter-apk/**/*.apk